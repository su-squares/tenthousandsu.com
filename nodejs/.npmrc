# Share dependencies across workspaces (saves disk space)
hoist=true

# Don't flatten to node_modules root (keeps structure clean)
shamefully-hoist=false

# Be strict about peer deps
strict-peer-dependencies=false
auto-install-peers=true
