# Playwright E2E Test Environment Configuration

# REQUIRED: Private key for test wallet (with or without 0x prefix)
PRIVATE_KEY=your_private_key_here

# Network to test against (Ethereum, Sepolia, or Sunet)
NETWORK=Sunet

# Chain ID (required only for Sunet custom networks)
# For Sunet local devnet: CHAIN_ID=99999991
# For Sepolia: CHAIN_ID=11155111
CHAIN_ID=99999991

# Custom RPC URL (to connect with Sunet, Sepolia or Mainnet)
RPC_URL=http://localhost:8545

# Optional: Base URL for tests (defaults to http://127.0.0.1:4000)
# BASE_URL=http://localhost:4000

# Optional: Wallet name shown in UI (defaults to MetaMask)
WALLET_NAME=MetaMask

# Optional: Delay between transactions in ms (defaults to 2000)
# TX_DELAY_MS=2000
