// Optional helper: expose Jekyll's `site.baseurl` to JavaScript.
// This pattern is valid and DRY for GitHub Pages and CNAME setups,
// but the Liquid placeholder `{{ site.baseurl }}` can trigger false-positive
// errors in some IDEs. In preference for a quieter editor experience,
// this helper is not used by default.
// If you want to use it, uncomment the line below and reference
// `window.SITE_BASEURL` in your scripts.
//
// window.SITE_BASEURL = "{{ site.baseurl }}";
